# Copy this file to `.env` and fill in your keys. Never commit `.env`.

# Used for PDF segmentation (slice 1) and 5.5e conversion (slice 2).
ANTHROPIC_API_KEY=

# Used for creature image generation via gpt-image-2 (slice 3).
OPENAI_API_KEY=
